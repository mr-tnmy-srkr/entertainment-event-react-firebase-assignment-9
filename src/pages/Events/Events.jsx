import React from 'react';
import Card from '../../components/Card/Card';

const Events = () => {
    return (
        <div>
        <Card></Card>
            events
        </div>
    );
};

export default Events;